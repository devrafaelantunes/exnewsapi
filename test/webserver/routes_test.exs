defmodule ExNews.Webserver.RoutesTest do
  @moduledoc """
    WIP.

    This module is still in development.
    My plan is to use the `bypass` dependency to mock the server and test its routes.

    'https://hex.pm/packages/bypass'
  """
end
