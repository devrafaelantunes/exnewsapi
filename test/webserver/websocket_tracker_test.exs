defmodule ExNews.Webserver.WebSocketTrackerTest do
  @moduledoc """
    WIP.

    This module is still in development.
    My plan is to test the WebSocketTracker's state following the same pattern I used in the `ExNews.StateTest` module.
  """
end
